# Secrets / API keys
agent/auth.json

# Transient session logs & telemetry
agent/sessions/
agent/bin/

# Cache files
.pi-lens/
agent/mcp-cache.json

# Runtime SQLite artifacts (transient)
memory/memory.db-shm
memory/memory.db-wal

# Context-mode knowledge base (indexed data)
context-mode/
